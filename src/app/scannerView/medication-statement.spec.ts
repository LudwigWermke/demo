import { MedicationStatement } from './medication-statement';

describe('MedicationStatement', () => {
  it('should create an instance', () => {
    expect(new MedicationStatement()).toBeTruthy();
  });
});
